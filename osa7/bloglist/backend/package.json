{
  "name": "blogilista",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "build:ui": "rimraf build && cd ../frontend && npm run build && xcopy dist ..\\backend\\build /E /I /Y"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.22.1",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.23.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^30.3.0",
    "nodemon": "^3.1.14",
    "rimraf": "^6.1.3",
    "supertest": "^6.3.4"
  }
}
